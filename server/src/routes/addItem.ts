import { FastifyInstance } from "fastify";

export default async function route (fastify: FastifyInstance){
    // TODO: fazer a rota de adicionar item
    fastify.get("/adicionar-item", async (req, res)=>{
        
    });
}